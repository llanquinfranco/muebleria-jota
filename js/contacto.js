import { cargarHeader, cargarFooter } from "./componentes.js";

cargarHeader();

const formulario = document.querySelector("#contacto-formulario");
const contenedorMensaje = document.querySelector("#mensaje-exito");

formulario.addEventListener("submit", function(evento) {
    evento.preventDefault();
    
    
    
    
});









cargarFooter();
